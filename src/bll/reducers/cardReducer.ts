const initialState = {}

export const CardReducer = (state = initialState, action: CardMainType) => {
    switch (action) {
        default:
            return state

    }
}

export type CardMainType = {}